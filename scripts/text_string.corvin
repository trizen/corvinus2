#!/usr/bin/ruby

var str = "acesta este un string";
spune str.caracterul(0);                # scire "a"
spune str.caracterul(1);                # scrie "c"

spune str.in_litere_mari;
spune str.capitalizeaza;

spune str.extrage(0, 6);

daca (str.contine("este")) {
    spune "Contine <este>";
}
