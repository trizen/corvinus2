#!/usr/bin/ruby

func mandelbrot(z) {
    var c = z;
    {   z = (z*z + c);
        z.abs > 2 && return adev;
    } * 20;
    return fals;
}

1 ^.. (-1, 0.05) -> fiecare { |y|
    -2 ..^ (0.5, 0.0315) -> fiecare { |x|
        scrie(mandelbrot(y.i + x) ? ' ' : '#');
    }
    scrie "\n";
}
